import os from 'os';

console.log(os.cpus()); //quantos cpus tem na máquina
console.log(os.freemem()); //memória ram livre
console.log(os.homedir()); //diretório principal da máquina
console.log(os.type()); //sistema operacional